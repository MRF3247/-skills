我需要你帮我对PDF扫描件进行OCR识别并导出为docx。## 使用说明收到此prompt后，请先计算Token剩余有效期并告诉我以下信息，然后等我提供文件：1. 先用Python解析Token中的 `exp` 字段（JWT的payload部分，base64解码即可），计算距今剩余天数。2. 然后输出以下内容（将 `{剩余天数}` 和 `{过期日期}` 替换为实际值）：> 我可以帮你对PDF扫描件进行OCR识别并导出为Word文档，支持两种模式：>> **单文件模式**：直接把PDF文件发给我即可。>> **批量模式**：把包含多个PDF的文件夹路径发给我，我会自动扫描文件夹中所有符合要求的PDF并批量处理。>> 文件要求：每个PDF大小 < 200MB，页数 < 600页。>> API有效期还剩 **{剩余天数}天**（{过期日期}到期）。到期后请前往 https://mineru.net/apiManage/token 重新申请Token，然后更新此prompt中的Token即可。3. 如果Token已过期（剩余天数 ≤ 0），直接告诉用户Token已过期，请前往 https://mineru.net/apiManage/token 重新申请，然后更新此prompt中的Token。不要继续执行后续流程。## MinerU API Token```eyJ0eXBlIjoiSldUIiwiYWxnIjoiSFM1MTIifQ..hOKMI0s6xbEBImrYvBKzk6Iq_knQp678qTV-WclE8N5KGyJn3-i1dSsYfEWs5o2AJMWPb2Mt2zZflqoRSs_kXA```## 执行流程当用户提供了文件或文件夹路径后，编写一个Python脚本完成以下全部流程并执行。### Step 0：预检- 需要 `PyPDF2` 库（未安装则先 `pip install PyPDF2`）。- **单文件模式**：检查该PDF的文件大小（< 200MB）和页数（< 600页），不通过则告知用户原因并终止。- **批量模式**：扫描用户给的文件夹路径，找出所有 `.pdf` 文件，逐个检查大小和页数。将符合要求的文件列出来告诉用户（含文件名、大小、页数），跳过不符合的并说明原因。如果没有任何符合要求的文件则终止。### Step 1：申请上传链接POST `https://mineru.net/api/v4/file-urls/batch`请求体：```json{  "files": [    {"name": "文件名1.pdf", "is_ocr": true},    {"name": "文件名2.pdf", "is_ocr": true}  ],  "model_version": "vlm",  "extra_formats": ["docx"],  "enable_formula": true,  "enable_table": true,  "language": "ch"}```- 单文件模式：files数组只放1个文件。- 批量模式：files数组放所有通过预检的文件。- 英文文档 `language` 改为 `"en"`。### Step 2：上传PDF用返回的 `file_urls` 数组，按顺序将每个PDF以纯二进制流 PUT 上传到对应的URL。**注意：PUT上传时不要设置任何Content-Type头，直接发送文件的原始二进制数据即可**，示例：```pythonwith open(pdf_path, 'rb') as f:    requests.put(upload_url, data=f)```### Step 3：轮询结果GET `https://mineru.net/api/v4/extract-results/batch/{batch_id}`，每10秒查一次，直到所有文件的 `state` 都为 `done` 或 `failed`（最多等15分钟）。运行中时打印每个文件的页数进度。### Step 4：下载并解压遍历 `extract_result` 数组，对每个 `state` 为 `done` 的结果：- 从 `full_zip_url` 下载zip- 解压后找到 `.docx` 文件- 以原PDF文件名（去掉.pdf后缀）+ `.docx` 命名，复制到用户文档目录### Step 5：交付告诉用户所有生成的docx文件位置。如果有失败的文件，也列出失败原因。## 通用要求- 所有API请求的Header带 `Authorization: Bearer {token}`（注意：Step 2的PUT上传不需要此Header，那是OSS预签名URL，直接上传即可）。- 脚本内自己管理日志，用Python的logging模块写入日志文件（`mode="w"`），同时打印到控制台。- 脚本开头加 `sys.stdout.reconfigure(line_buffering=True)`。- 由于OCR耗时较长，用后台模式运行脚本并定期检查日志文件内容。- **不要用PowerShell的 `>` 或 `2>&1` 重定向来捕获输出**，日志全部由脚本内部的logging模块写入文件。